elasticsearch:
  enabled: true
fluentbit:
  enabled: false
fluentd:
  enabled: true
  values:
    elasticsearch:
      logstashPrefix: logs
      host: elasticsearch-master
metricsServer:
  enabled: true
spec:
  destination:
    project: system
    server: https://kubernetes.default.svc
